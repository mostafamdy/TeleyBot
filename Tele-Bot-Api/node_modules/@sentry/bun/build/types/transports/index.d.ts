import type { BaseTransportOptions, Transport } from '@sentry/types';
export interface BunTransportOptions extends BaseTransportOptions {
    /** Custom headers for the transport. Used by the XHRTransport and FetchTransport */
    headers?: {
        [key: string]: string;
    };
}
/**
 * Creates a Transport that uses the Fetch API to send events to Sentry.
 */
export declare function makeFetchTransport(options: BunTransportOptions): Transport;
//# sourceMappingURL=index.d.ts.map