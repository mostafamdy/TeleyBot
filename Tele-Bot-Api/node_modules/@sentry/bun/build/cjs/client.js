Object.defineProperty(exports, '__esModule', { value: true });

const os = require('os');
const core = require('@sentry/core');

/**
 * The Sentry Bun SDK Client.
 *
 * @see BunClientOptions for documentation on configuration options.
 * @see SentryClient for usage documentation.
 */
class BunClient extends core.ServerRuntimeClient {
  /**
   * Creates a new Bun SDK instance.
   * @param options Configuration options for this SDK.
   */
   constructor(options) {
    core.applySdkMetadata(options, 'bun');

    const clientOptions = {
      ...options,
      platform: 'javascript',
      runtime: { name: 'bun', version: Bun.version },
      serverName: options.serverName || global.process.env.SENTRY_NAME || os.hostname(),
    };

    super(clientOptions);
  }
}

exports.BunClient = BunClient;
//# sourceMappingURL=client.js.map
